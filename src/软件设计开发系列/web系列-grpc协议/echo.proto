syntax = "proto3";

package echo;

// 定义服务接口
service EchoService {
    rpc BidirectionalStream(stream EchoRequest) returns (stream EchoResponse);
}

// 请求消息格式
message EchoRequest {
    string message = 1;
}

// 响应消息格式
message EchoResponse {
    string message = 1;
}